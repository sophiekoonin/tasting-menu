{
  "name": "tasting-menu",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {
    "@11ty/eleventy": "1.0.0"
  },
  "scripts": {
    "start": "BASE_URL=http://localhost:8080 eleventy --serve --quiet",
    "build": "eleventy"
  }
}